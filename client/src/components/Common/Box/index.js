import styled from "styled-components";
import {
  typography,
  space,
  color,
  flexbox,
  position,
  layout,
} from "styled-system";

const Box = styled.div`
  ${typography}
  ${space}
  ${color}
  ${flexbox}
  ${position}
  ${layout}
`;

export default Box;
